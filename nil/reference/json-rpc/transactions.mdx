# Transaction JSON-RPC reference

## `sendRawTransaction`

Creates a new message or a contract creation operation for a previously signed message.

### Inputs

| Parameter | Type | Description |
|:--:|:--:|:--|
| `encoded` | `Hash` | The bytecode representing the content of the message/contract code. |