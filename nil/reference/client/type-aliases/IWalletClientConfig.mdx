# Type Alias: IWalletClientConfig

```ts
type IWalletClientConfig: IClientBaseConfig & object;
```

The type representing the config for the basic wallet.

## Type declaration

### signer

```ts
signer: ISigner;
```

An instance of Signer is used to sign messages.
If a Signer is absent from the config, messages should be signed explicitly before being passed to the client.

#### Example

```ts
import { Signer } from '@nilfoundation/niljs';

const signer = new Signer();

const client = new WalletClient({
 endpoint: 'http://127.0.0.1:8529',
 signer: signer
})
```

## Defined in

[nil/nil.js/src/clients/types/ClientConfigs.ts:33](https://github.com/NilFoundation/nil.js/blob/08a7f90027b4c09b608245b19f15f6fb7ae1ff03/src/clients/types/ClientConfigs.ts#L33)
