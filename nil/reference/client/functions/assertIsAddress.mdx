# Function: assertIsAddress()

```ts
function assertIsAddress(address, message?): void
```

Checks if the address is valid. If the address is valid, it returns nothing.

## Parameters

<table>
<tr>
<th>Parameter</th>
<th>Type</th>
<th>Description</th>
</tr>
<tr>
<td>

`address`

</td>
<td>

`string`

</td>
<td>

The address to check.

</td>
</tr>
<tr>
<td>

`message`?

</td>
<td>

`string`

</td>
<td>

The message to throw if the address is invalid.

</td>
</tr>
</table>

## Returns

`void`

## Defined in

nil/nil.js/src/utils/assert.ts:140
