# Function: externalMessageEncode()

```ts
function externalMessageEncode(params, signer): Promise<object>
```

Encodes the given external message.

## Parameters

<table>
<thead>
<tr>
<th>Parameter</th>
<th>Type</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`params`

</td>
<td>

`Omit`\<[`ExternalMessage`](../type-aliases/ExternalMessage.mdx), `"authData"`\>

</td>
<td>

The external message to be encoded without its auth data.

</td>
</tr>
<tr>
<td>

`signer`

</td>
<td>

[`ISigner`](../interfaces/ISigner.mdx)

</td>
<td>

The message signer.

</td>
</tr>
</tbody>
</table>

## Returns

`Promise`\<`object`\>

The message bytecode and the message hash.

### hash

```ts
hash: Uint8Array;
```

### raw

```ts
raw: Uint8Array;
```

## Async

## Defined  
@nilfoundation/niljs/src/encoding/externalMessage.ts:262
